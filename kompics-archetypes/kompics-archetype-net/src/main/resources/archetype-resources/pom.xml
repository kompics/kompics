<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>${groupId}</groupId>
	<version>${version}</version>
	<artifactId>${artifactId}</artifactId>
	<packaging>jar</packaging>
	<name>${artifactId}</name>

	<properties>
	<!--  To change your version of Kompics, edit the kompics.version value below -->
			<kompics.version>${kompicsVersion}</kompics.version>
	</properties>
	
	<dependencies>
		<dependency>
			<groupId>${myGroup}</groupId>
			<artifactId>${myArtifact}</artifactId>
			<version>${myVersion}</version>
			<scope>compile</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<encoding>UTF-8</encoding>
					<source>1.6</source>
					<target>1.6</target>
					<debug>true</debug>
					<optimize>true</optimize>
					<showDeprecations>true</showDeprecations>
				</configuration>
			</plugin>
			<plugin>
				<artifactId>maven-assembly-plugin</artifactId>
				<configuration>
					<descriptorRefs>
						<descriptorRef>jar-with-dependencies</descriptorRef>
					</descriptorRefs>
					<archive>
						<manifest>
							<mainClass>${mainClass}</mainClass>
						</manifest>
					</archive>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>exec-maven-plugin</artifactId>
				<executions>
					<execution>
						<goals>
							<goal>java</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<executable>java</executable>
					<arguments>
<!--
This doesn't work in setting classpath. mvn exec:exec appears to work. 
						<argument>-classpath</argument>
						<classpath>target/${myProject}-${myVersion}-jar-with-dependencies.jar</classpath>
 -->						
						<argument>-classpath</argument>
						<classpath/>
						<argument>${mainClass}</argument>
					</arguments>
					<mainClass>${mainClass}</mainClass>
				</configuration>
			</plugin>			
		</plugins>
	</build>
</project>

